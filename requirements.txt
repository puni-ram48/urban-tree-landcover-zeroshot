# Requirements for SAM Segmentation + CLIP Classification Pipeline
# Install with: pip install -r requirements.txt --break-system-packages

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# PyTorch (CUDA support)
torch>=2.0.0
torchvision>=0.15.0

# Image processing
pillow>=9.0.0
opencv-python>=4.5.0
scikit-image>=0.19.0


# Visualization
matplotlib>=3.5.0

# System utilities
psutil>=5.8.0

# SAM (Segment Anything Model)
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git

# CLIP (Contrastive Language-Image Pre-training) 
transformers>=4.35.0

# Optional but recommended for better performance
# accelerate>=0.20.0  # For faster CLIP inference
# sentencepiece>=0.1.99  # For some CLIP tokenizers
